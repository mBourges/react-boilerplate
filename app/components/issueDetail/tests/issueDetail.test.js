/* eslint-disable no-unused-expressions, no-undef */

describe('A test suite', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('should fail2', () => {
    expect(true).to.be.false;
  });
});
